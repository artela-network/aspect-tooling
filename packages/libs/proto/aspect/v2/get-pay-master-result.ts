// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0
//   protoc        v4.23.2

import { Writer, Reader } from "as-proto/assembly";

export class GetPayMasterResult {
  static encode(message: GetPayMasterResult, writer: Writer): void {
    writer.uint32(10);
    writer.string(message.payMaster);
  }

  static decode(reader: Reader, length: i32): GetPayMasterResult {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new GetPayMasterResult();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.payMaster = reader.string();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  payMaster: string;

  constructor(payMaster: string = "") {
    this.payMaster = payMaster;
  }
}
