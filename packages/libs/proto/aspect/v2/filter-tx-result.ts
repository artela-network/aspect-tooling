// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0
//   protoc        v4.24.3

import { Writer, Reader } from 'as-proto/assembly';

export class FilterTxResult {
  static encode(message: FilterTxResult, writer: Writer): void {
    writer.uint32(8);
    writer.bool(message.isFilter);
  }

  static decode(reader: Reader, length: i32): FilterTxResult {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new FilterTxResult();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.isFilter = reader.bool();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  isFilter: bool;

  constructor(isFilter: bool = false) {
    this.isFilter = isFilter;
  }
}
