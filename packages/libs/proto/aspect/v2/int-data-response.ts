// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0
//   protoc        v4.23.2

import { Writer, Reader } from "as-proto/assembly";
import { RunResult } from "./run-result";

export class IntDataResponse {
  static encode(message: IntDataResponse, writer: Writer): void {
    const result = message.result;
    if (result !== null) {
      writer.uint32(10);
      writer.fork();
      RunResult.encode(result, writer);
      writer.ldelim();
    }

    writer.uint32(16);
    writer.int64(message.data);
  }

  static decode(reader: Reader, length: i32): IntDataResponse {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new IntDataResponse();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.result = RunResult.decode(reader, reader.uint32());
          break;

        case 2:
          message.data = reader.int64();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  result: RunResult | null;
  data: i64;

  constructor(result: RunResult | null = null, data: i64 = 0) {
    this.result = result;
    this.data = data;
  }
}
